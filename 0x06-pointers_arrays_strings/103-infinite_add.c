#include "main.h"
/**
 * rev_string - reverse array
 * @n: integer params
 * Return: 0
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int i = 0;
	int j = 0;
	char temp;

	while (*(n + i) != '0')
	{
		i++;
	}
	i++;
	for (j = 0; j < i; j++;
